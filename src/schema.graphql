type Query {
  person(id: ID!): Person
}
scalar DateTime
type Mutation {
  createSubmission(info:String!):Submission
}
input TimeFrameCreateInput {
  start:DateTime!
}

type Person {
  id: ID!
  name: String!
}

type TimeFrame {
  start: DateTime!
}

type Submission {
  id: ID!
  info: String!
}